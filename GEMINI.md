# 프로젝트 규칙 (Project Rules)

## 언어 방침
- 모든 답변, 설명, 커밋 메시지, 코드 주석 및 생성되는 모든 문서는 **100% 한글**로 작성합니다.

## 개발 가이드라인
- 로컬 개발 서버는 파이썬(`scripts/server.py`)을 기준으로 동작합니다.

## Git 워크플로우 규칙
- `git pull` 등으로 최신 코드를 받은 후에는 항상 최신 커밋 리스트 5개(`git log -n 5`)를 확인하여 사용자에게 보여줍니다.
